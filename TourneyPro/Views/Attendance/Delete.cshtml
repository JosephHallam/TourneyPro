@model TourneyPro_Data.Attendance

@{
    ViewBag.Title = "Delete";
}
    <style>
        #submitButton {
            border: 1px solid black;
            border-radius: 0.4rem;
        }

            #submitButton:hover {
                background-color: lightgreen;
            }
    </style>
<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Attendance</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Event.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Event.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.SiteUser.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SiteUser.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.isPlayer)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.isPlayer)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" id="submitButton" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
